{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "sh -c 'if [ -d \"apps/web\" ]; then npm --prefix apps/web run build; else npm run build && mkdir -p apps/web && cp -r dist apps/web/dist; fi'",
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
